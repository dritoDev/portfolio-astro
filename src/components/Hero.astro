
<!-- HERO -->
  <section id="home" class="hero">
    <h1>Hola, soy <span>Alejandro Rosa</span></h1>
    <h2>Frontend Developer</h2>
    <p>
      Desarrollo interfaces web modernas, accesibles y responsivas usando
      HTML, CSS y JavaScript.
    </p>
  </section>

<style>
.hero h1 span {
  color: var(--primary);
}

.hero-buttons {
  margin-top: 2rem;
  display: flex;
  gap: 1rem;
}

.btn {
  padding: 0.8rem 1.5rem;
  border-radius: 6px;
  text-decoration: none;
  font-weight: bold;
}

.primary {
  background: var(--primary);
  color: #000;
}

.secondary {
  border: 2px solid var(--primary);
  color: var(--primary);
}
  </style>